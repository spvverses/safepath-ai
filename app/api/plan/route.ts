import {NextResponse} from "next/server";import {plan} from "@/lib/providers";export const maxDuration=30;export async function POST(req:Request){try{const {origin,destination,preference}=await req.json();if(!origin||!destination)throw new Error("origin and destination are required");return NextResponse.json(await plan(origin,destination,preference));}catch(e){return NextResponse.json({error:String(e)},{status:502})}}